{
  "name": "mengenfaktor-kalkulation",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc && node scripts/copy-static.js",
    "dev": "tsc --watch",
    "test": "vitest"
  },
  "devDependencies": {
    "typescript": "^5.2.0",
    "vitest": "^1.0.0"
  }
}
